# Legacy provider artifacts

This directory contains historical Quafu task batches, raw task fragments, and
derived analysis payloads retained for paper reproduction. Directory placement
does not promote them to current product or release evidence.

Existing payloads are immutable migration records. New provider runs must use
`artifacts/development/` or an external evidence store rather than adding new
files here.
