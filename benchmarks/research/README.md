# Research scripts

Exploratory sweeps, figure generation, and one-off analysis belong here. They
may consume runner JSON payloads, but runners must never depend on this
directory. This one-way dependency keeps publication analysis from changing
the execution path used for reproducibility.

