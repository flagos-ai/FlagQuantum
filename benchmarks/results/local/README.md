# Local Results

This directory contains single-device fast-path benchmark results. These files
are local performance evidence only and are not distributed scalability claims.
Expected fields include `distribution_semantics="single_device_fast_path"`,
`scalability_claim_allowed=false`, and `release_gate_allowed=false`.
