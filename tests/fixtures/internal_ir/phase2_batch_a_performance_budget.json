{
  "schema_version": "1.0",
  "status": "active_private_regression_budget",
  "scope": "Phase 2 opt-in internal static compiler pipeline on local CPU",
  "evidence": "tests/fixtures/internal_ir/phase2_batch_a_performance_baseline.json",
  "derivation": {
    "latency": "max(original approved ceiling, 1.25 * max(full-pipeline p95, component-observation total p95)), rounded upward to a reviewable boundary",
    "memory": "max(original approved ceiling, 1.25 * measured tracemalloc peak), rounded upward to a binary reviewable boundary",
    "reason": "the original budget compared a complete import/pass/lower/verify pipeline against approximately twice legacy planning cost and was not compositionally attainable",
    "claim_boundary": "internal regression budget only; not a public SLA or throughput claim"
  },
  "budgets": [
    {
      "gate_count": 10,
      "pipeline_p95_ms_max": 1.25,
      "peak_host_memory_bytes_max": 1310720
    },
    {
      "gate_count": 100,
      "pipeline_p95_ms_max": 7.0,
      "peak_host_memory_bytes_max": 1310720
    },
    {
      "gate_count": 1000,
      "pipeline_p95_ms_max": 65.0,
      "peak_host_memory_bytes_max": 3145728
    },
    {
      "gate_count": 10000,
      "pipeline_p95_ms_max": 650.0,
      "peak_host_memory_bytes_max": 31457280
    }
  ],
  "additional_gates": {
    "semantic_parity": "unchanged from the original Phase 2 budget",
    "determinism": "exact output identity and pipeline digest across repeated runs",
    "gradient": "forward and gradient parity for every parameter-rewriting pass",
    "default_runtime_regression": "zero new-path imports or work without explicit internal opt-in",
    "budget_changes": "require new measured evidence and owner review; never auto-update to pass CI"
  }
}
