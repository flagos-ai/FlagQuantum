# Providers

Providers adapt external execution targets and compute platforms without
changing programs or owning Runtime scheduling. Start in `platform/` when
changing device discovery, lifecycle, identity, precision, or communication
capability observation.

This package does not own simulation numerics, compiler transforms, job
orchestration, user-facing result models, or vendor-neutral Core contracts.
