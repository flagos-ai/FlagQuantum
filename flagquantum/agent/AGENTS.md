# Agent Team Boundary

Agent APIs provide deterministic discovery, validation, planning, preflight,
execution, and explanation over public Core/Compiler/Runtime contracts. They must
not depend on an LLM, MCP transport, numerical kernels, concrete devices, or
provider internals.
