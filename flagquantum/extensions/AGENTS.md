# Ecosystem Team Boundary

Extensions are ecosystem-facing by default. Preserve the existing extension SDK
as the authority and do not create a second plugin registry. The nested `sdk/`
boundary is transitional Platform ownership and takes precedence.
